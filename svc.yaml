apiVersion: v1
kind: Service
metadata:
  name: capstone-service
spec:
  type: NodePort
  selector:
    app: capstone-app
  ports:
    - port: 5000        # inside-cluster port
      targetPort: 5000  # container port
      nodePort: 30007   # access via localhost:30007
